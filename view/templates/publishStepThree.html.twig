<!DOCTYPE html>
<html lang="en">
{% block head %}
{{ include("head.html.twig") }}
{% endblock %}
	<body>
{% block header %}
{% if connexionStatut is same as(true) %}
{{ include("header_connected.html.twig") }}
{% else %}
{{ include("header.html.twig") }}
{% endif %}
{% endblock %}
{% block nav %}
{% if connexionStatut is same as(true) %}
{{ include("nav_connected.html.twig") }}
{% else %}
{{ include("nav.html.twig") }}
{% endif %}
{% endblock %}
		<main>
			<section>
				<article id="publishFormThree">
					<form action="../../index.php?selectedTheme={{selectedTheme}}&pageNumber=6" method="post">
						<span style="color: red; align-self: center; margin-top: 33px;">{{publishCategoryError}}</span>
						<label for="categoryPublish">Choose a category :</label>
						<select name="categoryPublish">
						    <option value="" selected disabled hidden>--Please choose an option--</option>
						    {% for key, category in publishSections %}
						    <option value="{{key}}" {% if key == publishCategorySelected %}selected{% endif %}>{{category}}</option>
							{% endfor %}
						</select>
						<input type="submit" value="NEXT" name="submit">
						<input type="hidden" name="formSent" value="5">
						<a href="../../index.php?selectedTheme={{selectedTheme}}&pageNumber=5">GO BACK</a>
					</form>
				</article>
			</section>	
		</main>	
{% block footer %}
{{ include("footer.html.twig") }}
{% endblock %}
	</body>
</html>