<!DOCTYPE html>
<html lang="en">
{% block head %}
{{ include("head.html.twig") }}
{% endblock %}
	<body>
{% block header %}
{% if connexionStatut is same as(true) %}
{{ include("header_connected.html.twig") }}
{% else %}
{{ include("header.html.twig") }}
{% endif %}
{% endblock %}
{% block nav %}
{% if connexionStatut is same as(true) %}
{{ include("nav_connected.html.twig") }}
{% else %}
{{ include("nav.html.twig") }}
{% endif %}
{% endblock %}
		<main>
			<section>
				<article id="connexionForm">
					<form method="post" action="../../index.php?selectedTheme={{selectedTheme}}&pageNumber=3">
						<span style="color: red; text-align: center;">{{connexionError}}</span>
						<label id="login">{{site["login"]}}</label>
						<input type="text" name="login" id="login_form" required>
						<label id="password">{{site["password"]}}</label>
						<input type="password" name="password" id="password_form" required>
						<input type="submit" name="submit" value="CONNEXION">
						<input type="hidden" name="formSent" value="2">
						<a href="../../index.php?selectedTheme={{selectedTheme}}&pageNumber=1">You do not have an account yet ? Click here to registrer !</a>
					</form>
				</article>	
			</section>		
		</main>
{% block footer %}
{{ include("footer.html.twig") }}
{% endblock %}
	</body>
</html>